<?xml version="1.0" encoding="utf-8"?>
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- F — left vertical bar -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M15,27 L26,27 L26,81 L15,81 Z" />

    <!-- F — top horizontal bar (right edge angled → speed arrow upper edge) -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M15,27 L55,27 L62,38 L15,38 Z" />

    <!-- F — middle horizontal bar (angled right edge → arrow lower edge) -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M15,50 L44,50 L53,60 L15,60 Z" />

    <!-- 1 — top-left serif block -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M55,27 L65,27 L65,38 L55,38 Z" />

    <!-- 1 — main vertical stem -->
    <path
        android:fillColor="#FFFFFF"
        android:pathData="M65,27 L83,27 L83,81 L65,81 Z" />

</vector>
